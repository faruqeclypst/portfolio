// Simplified types for MicroCMS projects only
// The complex filtering types have been removed as we're using MicroCMS directly

// Re-export existing types for backward compatibility if needed
export type { Project, MetaPagination, PaginatedResponse } from "./api";
